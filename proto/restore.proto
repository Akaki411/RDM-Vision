syntax = "proto3";

package irec;

message RecoveryRequest {
    bytes image = 1;
    string mime_type = 2;
}

message RecoveryResponse {
    bytes image = 1;
    string mime_type = 2;
}

service Irec {
   rpc RecoveryImage(RecoveryRequest) returns (RecoveryResponse) {}
}
